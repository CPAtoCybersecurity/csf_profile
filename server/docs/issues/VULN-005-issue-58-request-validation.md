# VULN-005 – Missing / Weak Request Validation

**Related Issue:** #58

> **Discontinued.** VULN-005 covered request validation for the experimental Jira/Confluence
> API endpoints (`/api/jira/*`, `/api/confluence/*`). That live integration has been
> discontinued and relocated under `_future/`, so those endpoints are no longer part of the app.
>
> The validation test cases that used to live here therefore no longer apply. The app now
> retains CSV import/export only. This note is kept as a historical placeholder for issue #58
> (VULN-005).
