# Jira & Confluence Backend API — Testing Instructions

> **Discontinued.** The live Jira/Confluence API integration was experimental and has been
> discontinued. Its code and setup have been relocated under `_future/`, and the app now
> retains CSV import/export only.
>
> The endpoint testing instructions that used to live here (Atlassian credentials, the
> `/api/jira/*`, `/api/confluence/*`, and `/api/config/*` endpoints, and the Postman walkthrough)
> no longer apply to the app. This note is kept as a historical placeholder for issue #23.
